{
  "routes": [
    {
      "method": "POST",
      "path": "/runs/start",
      "handler": "publish-changes.runsStart",
      "config": {
        "policies": ["isSuperAdmin"]
      }
    },
    {
      "method": "GET",
      "path": "/runs/latest",
      "handler": "publish-changes.getRun",
      "config": {
        "policies": ["isSuperAdmin"]
      }
    }
  ]
}
